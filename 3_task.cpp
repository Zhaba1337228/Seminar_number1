#include <iostream>
#include <string>
#include <cmath>

int main()
{
    long long A,B;
    std::cout << "enter 2 numbers separated by a space: ";
    std::cin >> A >> B;
    std::cout << std::sqrt (A * A + B * B);
    return 0;
}
